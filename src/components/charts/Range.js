import React from "react";

export default function Range({ width }) {
  return (
    <div
      style={{ width: `${width}%` }}
      className="bg-gradient-to-r right-0 rounded-full h-4  from-yellow-400 via-cyan-300 to-emerald-400"
    >
      &nbsp;
    </div>
  );
}
